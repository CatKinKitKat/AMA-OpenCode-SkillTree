# Complete Requirement (example)

As an the-project operator, I want to create, retrieve, update, and delete
distribution lists (each with recipients) so that alerts reach the right
team.

Acceptance:
- Create returns the new list id.
- Find by name returns the list.
- Update changes recipients.
- Delete removes it (soft).

(Replace `the-project` / specifics with your own requirement.)
